/target
**/*.rs.bk
*.pdb

# Local config and state -- never commit a file that points at real directories.
/bowerbird.toml
/state.db
/locks/

# Model-comparison lab: real files, real results, never committed.
/lab/
